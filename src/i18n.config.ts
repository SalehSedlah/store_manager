
// This file is intentionally left empty or minimal as next-intl is being removed.
// It might be required by some build processes if referenced elsewhere,
// so keeping it minimal instead of deleting can prevent some "file not found" errors.
// Consider removing it completely if no longer referenced by any build script or configuration.

export const locales = ['ar']; // Minimal valid configuration
export const defaultLocale = 'ar'; // Minimal valid configuration
